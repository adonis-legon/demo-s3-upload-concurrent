spring.application.name=demo-s3-upload-concurrent

app.aws-profile=${APP_AWS_PROFILE}
app.aws-s3-bucket-name=${APP_AWS_S3_BUCKET_NAME}
app.total-temporal-files=${APP_TOTAL_TEMPORAL_FILES}
app.max-concurrent-uploads=${APP_MAX_CONCURRENT_UPLOADS}
app.http-max-connections=${APP_HTTP_MAX_CONNECTIONS}
app.http-connection-timeout=${APP_HTTP_CONNECTION_TIMEOUT}
app.http-connection-aquisition-timeout=${APP_HTTP_CONNECTION_AQUISITION_TIMEOUT}